<section class="hero section-shell">
  <div class="site-container hero-grid">
    <div class="hero-copy">
      <p class="eyebrow"><span></span> Web Engineer / Learning in public</p>
      <h1>こんにちは、<br /><em>imai</em>です 👋</h1>
      <p class="lead">
        フロントエンドとクラウドを中心に、試して、つまずいて、理解したことを書き残しています。
        完璧な解説よりも、明日もう一度使える実践メモを。
      </p>
      <div class="hero-actions">
        <a class="button-primary" href="/blog/">
          記事を読む
          <span aria-hidden="true">↗</span>
        </a>
        <a class="button-secondary" href="/about/">About me</a>
      </div>
      <div class="hero-tags" aria-label="主なテーマ">
        <span>#Astro</span><span>#JavaScript</span><span>#AWS</span><span>#Cloudflare</span>
      </div>
    </div>

    <div class="hero-visual" aria-label="Web開発のコードを表現したイラスト">
      <div class="orbit orbit-one"></div>
      <div class="orbit orbit-two"></div>
      <div class="code-card main-card">
        <div class="window-bar"><i></i><i></i><i></i><span>learning-log.ts</span></div>
        <pre><code><b>const</b> today = &#123;
  learn: <mark>"Astro"</mark>,
  build: <mark>"blog"</mark>,
  publish: <b>true</b>,
&#125;;</code></pre>
      </div>
      <div class="mini-card mini-card-one"><span>⚡</span><strong>Astro</strong><small>Islands</small></div>
      <div class="mini-card mini-card-two"><span>☁️</span><strong>Cloudflare</strong><small>Workers</small></div>
      <div class="status-pill"><i></i> currently learning</div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: clamp(4rem, 9vw, 8rem);
  }

  .hero-grid {
    display: grid;
    min-height: 520px;
    grid-template-columns: minmax(0, 1.04fr) minmax(360px, 0.96fr);
    align-items: center;
    gap: clamp(3rem, 7vw, 7rem);
  }

  .eyebrow {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin: 0 0 1.4rem;
    color: var(--muted);
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.11em;
    text-transform: uppercase;
  }

  .eyebrow span {
    width: 26px;
    height: 2px;
    border-radius: 99px;
    background: var(--brand);
  }

  h1 {
    max-width: 740px;
    margin: 0;
    color: var(--text);
    font-size: clamp(3.1rem, 7vw, 6.3rem);
    font-weight: 700;
    letter-spacing: -0.075em;
    line-height: 0.98;
  }

  h1 em {
    color: var(--brand);
    font-style: normal;
  }

  .lead {
    max-width: 630px;
    margin: 1.7rem 0 0;
    color: var(--muted);
    font-size: clamp(1rem, 1.6vw, 1.12rem);
    line-height: 2;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 2rem;
  }

  .hero-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem;
    margin-top: 2rem;
    color: var(--subtle);
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.78rem;
  }

  .hero-visual {
    position: relative;
    min-height: 430px;
    isolation: isolate;
  }

  .hero-visual::before {
    position: absolute;
    inset: 11% 7% 8% 10%;
    z-index: -3;
    border-radius: 42% 58% 55% 45% / 45% 38% 62% 55%;
    background: linear-gradient(145deg, #dfe4ff, #f4e7ff 54%, #ffe8dc);
    content: "";
    filter: saturate(0.9);
    transform: rotate(-5deg);
  }

  :global(.dark) .hero-visual::before {
    background: linear-gradient(145deg, #27315c, #3b294f 54%, #4d332d);
    opacity: 0.72;
  }

  .orbit {
    position: absolute;
    z-index: -2;
    border: 1px dashed color-mix(in srgb, var(--brand) 30%, transparent);
    border-radius: 50%;
  }

  .orbit-one {
    inset: 4% 0 2% 7%;
    transform: rotate(14deg);
  }

  .orbit-two {
    inset: 13% 10% 8% 0;
    transform: rotate(-18deg);
  }

  .code-card,
  .mini-card,
  .status-pill {
    border: 1px solid color-mix(in srgb, var(--line) 86%, transparent);
    background: color-mix(in srgb, var(--surface-raised) 92%, transparent);
    box-shadow: var(--shadow);
    backdrop-filter: blur(16px);
  }

  .main-card {
    position: absolute;
    top: 19%;
    left: 8%;
    width: min(90%, 440px);
    overflow: hidden;
    border-radius: 24px;
    transform: rotate(-2deg);
  }

  .window-bar {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    border-bottom: 1px solid var(--line);
    padding: 0.8rem 1rem;
    color: var(--subtle);
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.68rem;
  }

  .window-bar i {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #ff8a82;
  }

  .window-bar i:nth-child(2) { background: #ffca69; }
  .window-bar i:nth-child(3) { background: #65d99c; }
  .window-bar span { margin-left: auto; }

  pre {
    margin: 0;
    padding: 1.55rem;
    color: var(--text);
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: clamp(0.78rem, 1.4vw, 0.92rem);
    line-height: 1.9;
  }

  code b { color: var(--brand); }
  code mark {
    border-radius: 0.3rem;
    background: var(--brand-soft);
    color: var(--brand-strong);
    padding-inline: 0.25rem;
  }

  .mini-card {
    position: absolute;
    display: grid;
    min-width: 136px;
    grid-template-columns: auto 1fr;
    align-items: center;
    gap: 0 0.6rem;
    border-radius: 18px;
    padding: 0.85rem 1rem;
  }

  .mini-card span {
    grid-row: 1 / 3;
    font-size: 1.45rem;
  }

  .mini-card strong {
    font-size: 0.78rem;
    line-height: 1.25;
  }

  .mini-card small {
    color: var(--muted);
    font-size: 0.68rem;
  }

  .mini-card-one {
    top: 8%;
    right: 1%;
    transform: rotate(5deg);
  }

  .mini-card-two {
    right: 0;
    bottom: 7%;
    transform: rotate(-4deg);
  }

  .status-pill {
    position: absolute;
    bottom: 3%;
    left: 2%;
    display: flex;
    align-items: center;
    gap: 0.55rem;
    border-radius: 999px;
    padding: 0.62rem 0.9rem;
    color: var(--muted);
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.7rem;
  }

  .status-pill i {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #4fd18b;
    box-shadow: 0 0 0 5px rgb(79 209 139 / 0.14);
  }

  @media (max-width: 900px) {
    .hero-grid {
      min-height: 0;
      grid-template-columns: 1fr;
    }

    .hero-visual {
      min-height: 390px;
    }
  }

  @media (max-width: 520px) {
    h1 {
      font-size: clamp(2.9rem, 16vw, 4.4rem);
    }

    .hero-visual {
      min-height: 330px;
    }

    .main-card {
      top: 22%;
      left: 2%;
      width: 96%;
    }

    .mini-card {
      min-width: 116px;
      padding: 0.7rem 0.8rem;
    }
  }
</style>
