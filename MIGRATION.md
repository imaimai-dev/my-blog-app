# 現行版からの移行メモ

## 方針

現行コードを小さく継ぎ足すより、表示層を一度整理して差し替える方針です。Content Collections、OG画像生成、React/Svelte islands、Cloudflare Workers向けの静的配信という既存の決定事項は維持しています。

## 主な変更

- トップを中央寄せのタイトル中心構成から、2カラムのプロフィールヒーローへ変更
- ヘッダーを細くし、本文と同じ1120pxグリッドへ統一
- 記事カードの高さと余白を圧縮し、3列のZenn風グリッドへ変更
- Blogページに検索、タグ絞り込み、グリッド/リスト切り替えを集約
- フッターを固定感のある大きな領域から、通常フローのスリムな構成へ変更
- 色、罫線、影、余白をCSS変数で統一
- モバイルでは下部ナビゲーションへ切り替え

## 反映時の注意

現行リポジトリの依存関係と大きくは変えていませんが、`@astrojs/check` を追加しています。既存のlockfileを使い回さず、上書き後に `npm install` で再生成してください。
